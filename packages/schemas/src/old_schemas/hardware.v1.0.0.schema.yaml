$id: https://github.com/nqminds/Trusted-AI-BOM/blob/main/packages/schemas/src/hardware.v1.0.0.schema.yaml
$schema: https://json-schema.org/draft/2019-09/schema
title: Hardware Claim
description: |
  This claim will define the version of the underlying hardware component (e.g. GPUs) needed to run or train the system.
type: object
properties:
  id:
    type: string
    description: Unique ID of claim
  # TODO: implement
required: 
  - id